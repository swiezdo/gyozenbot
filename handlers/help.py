# /gyozenbot/handlers/help.py
import logging
from aiogram import Router
from aiogram.filters import Command
from aiogram.types import Message

router = Router()

# Настройка логирования
logger = logging.getLogger(__name__)


def _format_help_text() -> str:
    """
    Формирует текст справки с описанием всех доступных команд бота.
    
    Returns:
        str: Отформатированный текст справки в HTML формате
    """
    help_text = """<b>📖 Справка по командам бота</b>

<b>Команды с восклицательным знаком:</b>

<b>!волны</b>
Показывает скриншот списка волн для выживания в режиме "Кошмар" для текущей недели.

<b>!п</b>
Показывает профиль пользователя.
• Напишите <code>!п</code> - покажет ваш профиль
• Ответьте <code>!п</code> на сообщение - покажет профиль автора того сообщения

<b>!баланс</b>
Показывает баланс пользователя в Магатама 🪙
• Напишите <code>!баланс</code> - покажет ваш баланс
• Ответьте <code>!баланс</code> на сообщение - покажет баланс автора того сообщения

<b>Команды со слешем:</b>

<code>/build</code> или <code>/билд &lt;ID&gt;</code>
Показывает билд по указанному ID.
Пример: <code>/build 12</code> или <code>/билд 12</code>

<code>/help</code>
Показывает эту справку."""
    
    return help_text


@router.message(Command("help"))
async def help_command(message: Message):
    """
    Обработчик команды /help - показывает справку по всем доступным командам бота.
    """
    logger.info(f"Обнаружена команда /help от пользователя {message.from_user.id}")
    
    try:
        help_text = _format_help_text()
        await message.answer(help_text, parse_mode="HTML")
        logger.info("Справка отправлена пользователю %s", message.from_user.id)
    except Exception as e:
        logger.error(f"Ошибка при обработке команды /help: {str(e)}", exc_info=True)
        await message.reply("❌ Ошибка при показе справки")

